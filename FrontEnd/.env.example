# Backend API base. Defaults to http://localhost:5000/api.
REACT_APP_API_BASE=http://localhost:5000/api

# Alphavantage key — exposed to the client; this is a temporary measure until
# the backend proxies these calls. Rotate freely.
REACT_APP_ALPHAVANTAGE_KEY=
